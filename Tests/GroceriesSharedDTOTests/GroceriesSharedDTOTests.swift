import XCTest
@testable import GroceriesSharedDTO

final class GroceriesSharedDTOTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GroceriesSharedDTO().text, "Hello, World!")
    }
}
